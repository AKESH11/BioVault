{
  "name": "bio-vault-zkp",
  "version": "1.0.0",
  "description": "Zero-Knowledge Proof Circuits for Bio-Vault Protocol",
  "main": "index.js",
  "scripts": {
    "compile": "circom circuits/verify.circom --r1cs --wasm --sym -o build",
    "compile:all": "npm run compile:verify && npm run compile:bio",
    "compile:verify": "circom circuits/verify.circom --r1cs --wasm --sym -o build",
    "compile:bio": "circom circuits/bio_match.circom --r1cs --wasm --sym -o build",
    "setup": "snarkjs groth16 setup build/verify.r1cs powersOfTau28_hez_final_14.ptau build/verify_0000.zkey",
    "contribute": "snarkjs zkey contribute build/verify_0000.zkey build/verify_final.zkey --name='Bio-Vault Contribution'",
    "export-verifier": "snarkjs zkey export solidityverifier build/verify_final.zkey ../smart-contracts/contracts/Verifier.sol",
    "generate-proof": "node scripts/generate_proof.js",
    "verify-proof": "node scripts/verify_proof.js",
    "test": "node test/circuit_test.js"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.3"
  },
  "devDependencies": {
    "circom_tester": "^0.0.19",
    "chai": "^4.3.10"
  }
}
